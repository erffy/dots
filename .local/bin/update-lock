#!/usr/bin/env bash

FILE="/var/tmp/update.lock"

if [ -f $FILE ]; then
  echo "An update process is in progress, if you are sure that there is no update process you can delete the file."
  exit 1
fi

exit 0