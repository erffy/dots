{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$defs": {
    "BUFFER_SIZE": {
      "description": "Buffer size",
      "type": "integer"
    },
    "MAX_VERSION_LENGTH": {
      "description": "Maximum version length",
      "type": "integer"
    },
    "MAX_UPDATES": {
      "description": "Package updates to show in the tooltip",
      "type": "integer"
    }
  },
  "properties": {
    "BUFFER_SIZE": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "number"
        }
      ]
    },
    "MAX_VERSION_LENGTH": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "number"
        }
      ]
    },
    "MAX_UPDATES": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "number"
        }
      ]
    }
  }
}