{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$defs": {
    "TARGET": {
      "description": "Target Ping URL",
      "type": "string"
    },
    "PACKET_SIZE": {
      "description": "Packet size",
      "type": "integer"
    },
    "TIMEOUT": {
      "description": "Ping timeout",
      "type": "integer"
    }
  },
  "properties": {
    "TARGET": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "number"
        }
      ]
    },
    "PACKET_SIZE": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "number"
        }
      ]
    },
    "TIMEOUT": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "number"
        }
      ]
    }
  }
}